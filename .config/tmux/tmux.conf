# Unbind default prefix and set new one
unbind C-b
set -g prefix C-s
bind C-s send-prefix

# Quick config reload
unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display-message "Config reloaded!"

# Terminal and color support
set-option -sa terminal-overrides ",xterm*:Tc"
set -g default-terminal "screen-256color"

# Mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Act like vim
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Vim-style copy mode
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Better window splitting (more intuitive)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# Window navigation
bind -n M-H previous-window
bind -n M-L next-window

# Session management
bind C-c new-session
bind C-f command-prompt -p find-session 'switch-client -t %%'

# Pane resizing
bind -r H resize-pane -L 2
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 2

# Performance and responsiveness
set -g escape-time 0
set -g repeat-time 300
set -g focus-events on

# History and scrollback
set -g history-limit 50000

# Visual improvements
set -g automatic-rename on
set -g renumber-windows on
set -g set-titles on
set -g set-titles-string "#I:#W"

# Status bar position
set -g status-position top

# === ACTIVE THEME: DRACULA ===
set -g @plugin 'dracula/tmux'


# set -g @plugin 'xamut/tmux-weather'

# Configure it to show condition + temperature:
set -g @tmux-weather-location "Bangalore" 
set -g @dracula-show-location false
set -g @tmux-weather-format "%C+%t+%C"
set -g @tmux-weather-interval 15

# Dracula theme configuration
set -g @dracula-show-powerline true
# set -g @dracula-show-weather true
set -g @dracula-fixed-location "Bengaluru"
# set -g @dracula-plugins "cpu-usage battery"
set -g @dracula-plugins "cpu-usage battery ram-usage weather"
set -g @dracula-narrow-plugins "compact-alt battery network weather"

set -g @dracula-show-powerline true
set -g @dracula-transparent-powerline-bg true



# Remove weather from dracula plugins
# set -g @dracula-plugins "cpu-usage battery"

# # Add custom weather status that shows text instead of emoji
# # set -gF status-right "#{E:@dracula-show-flags}#{E:@dracula-weather}#{E:@dracula-show-right-sep}#[fg=#ffb86c]#(curl -s 'wttr.in/Bangalore?format=%C+%t&m' | sed 's/%//')#[default] #{E:@dracula-cpu-usage} #{E:@dracula-battery}"
# set -g status-right-length 100
# set -g status-left "#[fg=#bd93f9]#{?client_prefix,🚀 ,}#S "
# # set -g status-right "#[fg=#ffb86c]#(curl -s 'wttr.in/Bangalore?format=%C+%t&m' | sed 's/%//') #[fg=#bd93f9]#{?client_prefix,🚀 ,}#S "
# set -g status-right "#[fg=#ffb86c]#(curl -s 'wttr.in/Bangalore?format=%C+%t&m' | sed 's/%//') #[fg=#50fa7b]CPU #{cpu_percentage} #[fg=#ff79c6]BAT#{battery_percentage}"

# set -g automatic-rename-format "#(curl -s 'wttr.in/Bangalore?format=%C+%t&m' | sed 's/%//') - #{b:pane_current_path}"
# Configure the weather plugin
set -g @tmux-weather-location "Bangalore"
set -g @tmux-weather-format "%c+%t"
set -g @tmux-weather-interval 1800  # Update every 30 minutes
# set -g status-right "#[fg=#ffb86c]#{weather} #[fg=#50fa7b]CPU #{cpu_percentage} #[fg=#ff79c6]BAT#{battery_percentage}"
# set-option -g status-right "#{weather}"

set -g status-interval 60
WEATHER='#(curl -s wttr.in/Bangalore\?format\="%%l:+%%c%%20%%C%%20%%t%%60%%w&period=60")'
set -g status-right "$WEATHER ..."

# set -g status-right-length 150
# set -g status-right "#[fg=#ffb86c,bold]#(curl -s 'wttr.in/Bangalore?format=%C+%t&m' | sed 's/%//') #[fg=#50fa7b]CPU #{cpu_percentage} #[fg=#ff79c6]BAT#{battery_percentage} #[fg=#6272a4]%H:%M"

set -g @dracula-show-flags true
set -g @dracula-show-left-icon session
set -g @dracula-show-fahrenheit false
set -g @dracula-refresh-rate 5
set -g @dracula-cpu-usage-label "CPU"
set -g @dracula-battery-label "BAT"

# === INACTIVE THEME: CATPPUCCIN (commented out) ===
# set -g @plugin 'catppuccin/tmux'
# set -g @catppuccin_flavour 'mocha'
# set -g @catppuccin_window_left_separator ""
# set -g @catppuccin_window_right_separator " "
# set -g @catppuccin_window_middle_separator " █"
# set -g @catppuccin_window_number_position "right"
# set -g @catppuccin_window_default_fill "number"
# set -g @catppuccin_window_default_text "#W"
# set -g @catppuccin_window_current_fill "number"
# set -g @catppuccin_window_current_text "#W"
# set -g @catppuccin_status_modules_right "directory cpu battery"
# set -g @catppuccin_status_left_separator " "
# set -g @catppuccin_status_right_separator ""
# set -g @catppuccin_status_fill "icon"
# set -g @catppuccin_status_connect_separator "no"

# Other plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# Session persistence
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
set -g @continuum-boot 'on'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.config/tmux/plugins/tpm/tpm'
